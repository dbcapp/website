'use strict';

const express = require('express');
const passport = require('passport');
const controller = require('../../controllers/api/organization');
const router = express.Router();

module.exports = router;